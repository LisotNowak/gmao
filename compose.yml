networks:
  gmao-network:
  npm-network:   # 👈 ajout pour se brancher au réseau NPM
    external: true   # car déjà créé par Nginx Proxy Manager

volumes:
  pgdata:

services:
  db:
    image: postgres:15-alpine
    container_name: gmao-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: D@n@kil31989
      POSTGRES_DB: gmao
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - gmao-network   # 👈 reste interne, pas exposé à NPM

  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
      args:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: D@n@kil31989
        POSTGRES_DB: gmao
    env_file:
      - ./back/.env
    container_name: gmao-backend    
    environment:
      NODE_ENV: production
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: D@n@kil31989
      POSTGRES_DB: gmao       
    expose:         # 👈 uniquement exposé en interne
      - "3000"
    depends_on:
      - db
    networks:
      - gmao-network
      - npm-network   # 👈 dispo dans NPM

  frontend:
    build:
      context: .
      dockerfile: ./front/Dockerfile
      args:
        VITE_API_URL: "/api"   # 👈 proxy via nginx
    container_name: gmao-frontend
    expose:         # 👈 exposé au réseau interne
      - "80"
    depends_on:
      - backend
    networks:
      - gmao-network
      - npm-network   # 👈 dispo dans NPM
